program pdfiumfmx;

uses
  System.StartUpCopy,
  FMX.Forms,
  FileView in 'FileView.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
